# Stone House Site

Standalone Next.js marketing site for Vercel deployment.

## Run locally

1. Install Node.js
2. Run `npm install`
3. Run `npm run dev`

## Deploy

1. Push this folder to a GitHub repository
2. Import the repo into Vercel
3. Use the default Next.js build settings
